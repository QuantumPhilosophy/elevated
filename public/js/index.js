'use strict'

// Add event listeners
$(document).ready(function () {
  $('.sidenav').sidenav({
    menuWidth: 100,
    edge: 'right'
  })
})

$(document).ready(function () {
  $('.collapsible').collapsible()
})
